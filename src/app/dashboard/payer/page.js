import PayerDashboard from "@/components/PayerDashboard";

export const metadata = {
  title: "Payer Dashboard | CRED Split",
  description: "Review and settle your assigned group expenses, track balances, and submit payment proof.",
};

export default function DashboardPayerPage() {
  return <PayerDashboard />;
}
