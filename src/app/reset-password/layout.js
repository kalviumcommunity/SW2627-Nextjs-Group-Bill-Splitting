export const metadata = {
  title: "Reset Password | CRED Split",
  description: "Enter your reset code and set a new password for your CRED Split account.",
};

export default function ResetPasswordLayout({ children }) {
  return children;
}
